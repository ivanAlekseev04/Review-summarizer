{
    "name": "review-summarizer",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "devDependencies": {
        "@types/bun": "latest",
        "concurrently": "^10.0.3",
        "prettier": "^3.8.4"
    },
    "scripts": {
        "dev": "bun run index.ts",
        "format": "prettier --write ."
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "workspaces": [
        "packages/*"
    ],
    "dependencies": {
        "dotenv": "^17.4.2",
        "zod": "^4.4.3"
    }
}
